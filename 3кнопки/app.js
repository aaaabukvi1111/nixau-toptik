function first() {
    // Про 10+50 кнопка
    alert(`${505+505}`)
    }
    function second() {
    // prompt() -  можем записать данные в переменную
  name = prompt(alert(name))
    }
    function third() {
    // Вставить в элемент HTML с id 'zp' вставить тысячу
    document.getElementById('zp').innerHTML = '1$';
    }
    